更新了“regfile"同周期写读问题，现在可以支持同周期转发。
always_comb begin
    ex_if_bus.branch_sign = 1'b0;
    ex_if_bus.pc_branch   = 32'b0;
    branch_sign           = 1'b0;  // 你的中间变量也给个初值

//先在前面赋值0，（而不是后面的else）后面if给需要调整的量，避免if嵌套丢失了情况。